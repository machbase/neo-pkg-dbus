---
version: alpha
name: Machbase Neo Package Starter
description: Dense dark operations UI generated by neo-pkg init for jobs and single-service packages.
colors:
  primary: "#005fb8"
  selected-surface: "#0053a0"
  interactive-hover: "#0075e2"
  surface: "#1e1e1e"
  input-surface: "#353535"
  panel: "#252525"
  elevated: "#2c2c2c"
  text: "#f1f1f1"
  text-secondary: "#c4c4c4"
  text-muted: "#a3a3a3"
  border: "rgba(255, 255, 255, 0.13)"
  success: "#71e071"
  warning: "#f5c451"
  error: "#ff5353"
typography:
  body:
    fontFamily: "Pretendard, sans-serif"
    fontSize: 13px
    fontWeight: 400
    lineHeight: 1.5
  button:
    fontFamily: "Pretendard, sans-serif"
    fontSize: 12px
    fontWeight: 600
  side-title:
    fontFamily: "Pretendard, sans-serif"
    fontSize: 12px
    fontWeight: 600
    lineHeight: 1
  detail-title:
    fontFamily: "Pretendard, sans-serif"
    fontSize: 17px
    fontWeight: 600
    lineHeight: 1.35
  card-title:
    fontFamily: "Pretendard, sans-serif"
    fontSize: 15px
    fontWeight: 700
    lineHeight: 1
    letterSpacing: 0.12em
  metric-label:
    fontFamily: "Pretendard, sans-serif"
    fontSize: 12px
    fontWeight: 600
    letterSpacing: 0.05em
  metric-value:
    fontFamily: "Pretendard, sans-serif"
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.35
  code:
    fontFamily: "D2Coding, monospace"
    fontSize: 11px
    fontWeight: 400
    lineHeight: 1.6
spacing:
  icon-gap: 4px
  compact-gap: 6px
  small-gap: 8px
  title-gap: 10px
  compact-edge: 12px
  message-edge: 14px
  grid-gap: 16px
  section-gap: 24px
  card-padding: 32px
  page-edge: 40px
rounded:
  none: 0px
  control: 4px
  pill: 9999px
components:
  content-shell:
    backgroundColor: "{colors.panel}"
    textColor: "{colors.text}"
  button:
    backgroundColor: transparent
    textColor: "{colors.text-secondary}"
    typography: "{typography.button}"
    rounded: "{rounded.control}"
    height: 28px
  button-hover:
    backgroundColor: "{colors.interactive-hover}"
    textColor: "#ffffff"
    rounded: "{rounded.control}"
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "#ffffff"
    rounded: "{rounded.control}"
    height: 32px
  input:
    backgroundColor: "{colors.input-surface}"
    textColor: "{colors.text}"
    rounded: "{rounded.control}"
    height: 32px
  side-panel:
    backgroundColor: "{colors.panel}"
    width: 256px
  summary-card:
    backgroundColor: "{colors.surface}"
    rounded: "{rounded.control}"
    padding: "{spacing.card-padding}"
  empty-state:
    backgroundColor: "{colors.elevated}"
    textColor: "{colors.text-secondary}"
    rounded: "{rounded.control}"
    padding: "{spacing.section-gap}"
  metric-label:
    textColor: "{colors.text-muted}"
    typography: "{typography.metric-label}"
  status-running:
    textColor: "{colors.success}"
    rounded: "{rounded.control}"
  status-warning:
    textColor: "{colors.warning}"
  status-error:
    textColor: "{colors.error}"
  switch-track:
    backgroundColor: "#3a3a3a"
    rounded: "{rounded.pill}"
    width: 28px
    height: 13px
  switch-track-on:
    backgroundColor: "{colors.primary}"
    rounded: "{rounded.pill}"
    width: 28px
    height: 13px
  result-json:
    backgroundColor: "{colors.surface}"
    textColor: "#9ecbff"
    typography: "{typography.code}"
    rounded: "{rounded.control}"
---

# Machbase Neo Package Starter Design

## Overview

`neo-pkg init`이 만드는 화면은 패키지의 실행 상태와 작업을 관리하는 조밀한 운영 도구다. 현재 검증한 jobs + side 구성은 왼쪽에서 작업을 고르고 켜거나 끄며, 오른쪽에서 상세 정보와 생성·수정 화면을 보여 준다. 장식보다 많은 정보, 빠른 상태 구분, 좁은 공간에서도 끊기지 않는 조작을 우선한다.

## Colors

- `surface`는 페이지 바탕, 요약 카드와 결과 영역에 쓰는 가장 어두운 층이다. 입력과 선택 컨트롤은 이보다 밝은 `input-surface`를 사용해 조작 영역을 구분한다.
- Side에서 선택된 작업 행은 `selected-surface`를 사용한다. 이는 주요 동작 버튼의 `primary`보다 어두워, 선택 상태를 유지하면서도 조작 버튼과 구분된다.
- `panel`은 Side와 Main의 기본 작업 영역이고, `elevated`는 빈 상태와 보조 상태 영역을 한 단계 분리한다.
- `primary`는 선택된 작업, 켜진 스위치, 기본 동작 버튼에 쓴다. `interactive-hover`는 hover와 2px 포커스 윤곽선, 패키지 아이콘에 쓴다.
- 기본 글자는 `text`, 보조 설명과 버튼은 `text-secondary`, 낮은 우선순위 레이블은 `text-muted`를 쓴다.
- 성공·경고·오류는 각각 `success`, `warning`, `error`로 구분한다. 시작·정지 중 상태에는 밝은 파랑 `#74b9ff`, 상세 오류에는 연한 빨강 `#ffaaa8`, JSON에는 연한 파랑 `#9ecbff`를 쓴다.
- 모든 주요 구분선은 1px `border`를 사용한다. 이 값은 실제 CSS 사용자 정의 속성이지만 공식 컴포넌트 토큰 형식에 경계선 색 속성이 없어 YAML 컴포넌트에서 직접 참조하지 않는다.

## Typography

일반 UI는 `Pretendard, sans-serif`, 기술 값과 JSON은 `D2Coding, monospace`를 사용한다. 두 글꼴은 각 HTML entry가 고정된 jsDelivr 주소에서 불러오며, CDN을 사용할 수 없으면 각각 `sans-serif`와 `monospace`로 대체한다. 기본 본문은 13px/1.5이고 일반 버튼과 입력은 12px를 사용한다.

Side 패키지 제목은 12px, 굵기 600, 줄 높이 1이며 긴 이름은 한 줄 말줄임표로 처리한다. 상세 제목은 17px/1.35, 굵기 600이다. 카드 제목은 15px, 굵기 700, 자간 0.12em의 대문자이고 메트릭 레이블은 12px, 굵기 600, 자간 0.05em의 대문자다. 메트릭 값은 16px/1.35이며 큰 count 값은 32px까지 커진다.

Material Symbols Outlined 아이콘은 기본 18px이고 작은 동작에서는 14px다. 아이콘 글꼴 설정은 `"FILL" 0, "wght" 400, "GRAD" 0, "opsz" 20`을 유지한다.

## Layout

jobs + side 미리보기는 256px Side와 남은 너비를 쓰는 Main의 두 열 그리드다. Side 헤더는 40px, 목록 헤더는 22px, 작업 행은 28px 높이다. 각 패널은 전체 높이를 채우며 가로 넘침은 숨기고 필요한 영역만 세로 스크롤한다.

Main의 상세·생성 헤더는 고정 높이와 최소 높이가 모두 80px이다. 헤더의 좌우 여백은 40px이며 버튼 묶음은 6px 간격이다. jobs 상세 메트릭은 3열, single 상세 메트릭은 2열이다. 작업 생성 폼은 32px 40px 40px의 몸통 패딩, 32px 카드 패딩, 16px 간격의 2열 입력 그리드를 사용한다.

모든 그리드 자식과 긴 텍스트 영역은 `min-width: 0`을 유지한다. 긴 이름은 말줄임표 또는 `overflow-wrap: anywhere`로 처리하고 JSON은 가로 스크롤과 줄바꿈을 함께 지원한다.

## Elevation & Depth

깊이는 세 가지 어두운 표면과 1px 반투명 구분선으로 만든다. 카드와 폼에는 큰 드롭 섀도를 쓰지 않는다. 유일한 작은 그림자는 스위치 손잡이의 `0 1px 2px rgba(0, 0, 0, 0.4)`이며, 이는 작은 조작부의 앞뒤 관계만 보여 준다.

## Shapes

버튼, 입력, 카드와 상태 배지는 모두 4px 모서리를 사용한다. 큰 작업 영역과 패널 경계는 각진 모서리를 유지한다. 스위치 트랙과 9px 손잡이는 9999px 반경으로 완전한 알약 또는 원 모양을 만든다.

## Components

- **기본 버튼:** 최소 높이 28px, 패딩 0 10px, 아이콘 간격 4px이다. hover에서는 파란 배경과 흰 글자로 바뀌며 disabled에서는 투명도 0.45와 `not-allowed` 커서를 사용한다.
- **주요 동작 버튼:** 생성·저장 같은 핵심 동작은 32px 높이, 패딩 0 16px, `primary` 배경을 사용한다.
- **아이콘 버튼:** Back은 32px 정사각형, Side의 New Job은 28px, Refresh는 22px 정사각형이다. Side 아이콘 버튼 hover는 흰색 10% 배경만 더한다.
- **입력:** 높이 32px, 패딩 0 9px, 1px 경계선, 4px 모서리다. 폼 레이블은 12px 굵기 600이며 레이블과 입력 간격은 6px이다.
- **작업 목록:** 행 높이는 28px이다. 선택 행은 `primary`, hover 행은 `interactive-hover`를 사용하고 이름은 한 줄 말줄임표로 유지한다.
- **스위치:** 전체 클릭 영역은 28px 정사각형이고 실제 트랙은 28px × 13px, 손잡이는 9px다. 켜지면 트랙이 `primary`, 손잡이가 흰색으로 바뀐다.
- **상태 메시지:** 패딩 10px 14px, 12px/1.5이며 Main과 Side 위쪽에 표시된다. 일반·경고·오류의 텍스트 색을 구분한다.
- **상태 배지:** 패딩 4px 10px과 4px 모서리를 사용한다. RUNNING, STARTING, STOPPING, STOPPED, FAILED, ERROR, NOT INSTALLED, UNKNOWN을 읽을 수 있는 텍스트와 색으로 보여 준다.
- **요약 카드:** 최소 높이 224px, 패딩 32px, 어두운 `surface` 배경을 사용한다. 결과 카드는 같은 표면과 32px 패딩을 사용한다.
- **위험 버튼:** 삭제는 기본 `#ff4747` 배경과 `#fa6464` 경계선을 사용하고 hover에서 `error`로 진해진다.

## OPC UA DataViewer 공통 프로필

CCR-052 승인에 따라 DataViewer는 `neo-pkg-opcua-client`의 원본 화면을 공통으로 쓴다. 그래서 DataViewer 안에서는 원본의 4px 간격, 32px control, `#1e1e1e`/`#252525`/`#2c2c2c` 표면, `#353535` 입력 표면, `#005fb8` 기본색, `rgba(255,255,255,.13)` 경계선을 그대로 사용한다. 이 값은 위 공통 토큰과 같다.

원본에서 DataViewer 전용으로 필요한 보조 표면은 modal `#2a2a2a`, control 경계 `#3e3e3e`, chart control 경계 `#454545`, hover 경계 `#5a5a5a`다. 이들은 시간 범위 modal과 차트 control 안에서만 사용한다. Tags/Asset/Derived 목록, Raw Grid, split chart, zoom/pan, timezone 표시 선택, Tag Analyzer 연결은 `frontend/src/data-viewer/opcua-data-viewer.css`의 원본 규칙을 기준으로 유지한다.

## Do's and Don'ts

- Do jobs와 single 화면 모두 같은 `--neo-*` 토큰과 공용 상태 컴포넌트를 사용한다.
- Do 상태를 색만으로 구분하지 말고 RUNNING, FAILED 같은 텍스트와 `aria-pressed` 상태를 함께 제공한다.
- Do Side 너비 256px과 40px/22px/28px의 조밀한 세로 리듬을 유지한다.
- Do `height`와 `min-height`, 축별 패딩, `min-width: 0`을 원본 의미 그대로 유지한다.
- Don't 카드에 큰 그림자나 큰 둥근 모서리를 추가하지 않는다.
- Don't JSON과 기술 식별자에 일반 산세리프 글꼴을 사용하지 않는다.
- Don't Side 스위치와 Main의 시작·정지 버튼을 같은 구성에서 중복해 보여 주지 않는다.
- Don't 오류 메시지를 정상 디자인 예시처럼 고정하지 않는다. 오류 종류와 HTTP 상태를 실제 응답에 따라 표시한다.

## Responsive Behavior

640px 이하에서 standalone Main이나 Side 없는 인덱스의 상세 헤더·동작 영역은 한 열로 바뀌고, 동작 버튼은 다음 행의 왼쪽에 5px 16px 패딩으로 놓인다. 해당 구성의 메트릭과 모든 생성 폼 입력 그리드는 한 열로 줄어든다.

jobs + side 통합 미리보기는 600px에서도 256px Side와 나머지 Main의 두 열을 유지하는 것을 확인했다. 이때 폼 입력은 한 열로 줄고, Main 제목은 말줄임표가 되며 문서 전체의 가로 스크롤은 생기지 않았다. 통합 split 레이아웃 자체를 한 열로 쌓는 미디어 규칙은 현재 없다.

## Accessibility

Side는 `aside`, Main은 `main` 랜드마크이고 Jobs·Service 영역에는 접근 가능한 이름이 있다. 아이콘 버튼은 `aria-label`과 `title`을 제공하고 장식용 Material Symbols는 `aria-hidden="true"`다. 작업 선택과 스위치는 `aria-pressed`로 시각 상태와 접근성 상태를 맞춘다.

오류는 `role="alert"`, 정리 경고는 `role="status"`, 첫 로딩은 `aria-live="polite"`로 알린다. 버튼·입력·button 역할 요소의 키보드 포커스는 `interactive-hover` 색 2px 윤곽선으로 표시한다. 사용할 수 없는 동작은 실제 `disabled` 속성과 0.45 투명도를 함께 쓴다.

## Motion

일반 레이아웃에는 애니메이션을 쓰지 않는다. 스위치 트랙의 배경·경계선과 손잡이 위치·배경만 120ms ease로 전환한다. 이 짧은 움직임은 켜짐과 꺼짐 상태 변화를 설명하는 데만 사용한다.

## Themes

현재는 어두운 테마 한 가지만 있다. 밝은 테마 전환이나 사용자 선택 테마는 정의되어 있지 않다.

## Agent Guidance

디자인을 바꿀 때는 실제 init 생성물의 `--neo-*` 사용자 정의 속성과 공용 컴포넌트를 먼저 수정한다. jobs와 single, Main과 Side가 같은 토큰을 공유하므로 한 화면만 고치고 끝내지 않는다. `StarterView`의 상태 메시지와 모드 분기, BroadcastChannel을 통한 Main/Side 선택 동기화, 작업 상태별 disabled 규칙은 시각 변경 중에도 보존한다.

## Evidence and Coverage

분석 대상은 `neo-pkg init`으로 생성된 React/Vite 프런트엔드다. jobs + side 미리보기를 제공된 로컬 주소에서 1280px 및 600px 너비로 확인했고, CGI 404 상태와 New Job 폼을 실제로 열어 적용 스타일을 검증했다. jobs의 정상 데이터 상세와 single 모드는 생성 소스와 CSS로 확인했지만 실행 화면으로는 검증하지 않았다.
